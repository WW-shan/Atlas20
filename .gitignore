# Python
.venv/
__pycache__/
.pytest_cache/
*.py[cod]
*.egg-info/

# Local tooling
.env
!.env.example
.superpowers/
.playwright-mcp/
output/playwright/

# Node
node_modules/
dist/
*.tsbuildinfo

# OS
.DS_Store
Thumbs.db

# Local AI / worktree tooling
.worktrees/
.codex/

# Generated app runs
reports/app_runs/*
!reports/app_runs/.gitkeep
reports/latest.txt

# Bulky regenerable scan output; the summary/stability/yearly CSVs are kept
reports/bull_offense_scan/bull_offense_equity_curves.csv

# Runtime artifacts under data/
data/atlas20.sqlite
data/atlas20.sqlite-journal
data/atlas20.sqlite-wal
data/atlas20.sqlite-shm
data/.scheduler.lock
data/.prom-multiproc-worker/
data/.prom-multiproc-api/

# Pipeline outputs (regeneratable from data/raw + config); used to be tracked
# but ~47MB of CSV is too heavy for git.
data/processed/

# Raw provider snapshots (coinmarketcap history + coingecko catalog/metadata);
# deterministic given API + time, but ~47MB total. Re-fetch via
# download_and_cache_raw_data.
data/raw/

# Design / imagegen mockups; kept locally for reference, not in version
# control (each ~1.5MB; 21 PNGs).
output/imagegen/
output/load/
output/full-functional/

.ace-tool/

# CCG task artifacts (local only, not uploaded)
.ccg/
